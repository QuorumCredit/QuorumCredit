# Build artifacts
/target
**/target
target/
*.wasm
*.wasm.gz

# Windows installer binaries
*.exe

# Environment files
.env
.env.*
!.env.example
.env.local
.env.*.local

# Test snapshots (keep all snapshot formats out of repo)
test_snapshots/
**/test_snapshots/
**/*.snap
**/__snapshots__/
**/snapshots/
**/*.snapshot
**/snapshot_tests/
# Insta (Rust snapshot testing)
**/*.snap.new
# Jest / Vitest snapshots
**/__snapshots__/**
**/*.test.snap

# WASM build artifacts
*.wasm
*.wasm.opt

# Windows installer binaries
*.exe

# Backup files
*.bak
*~

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*.iml
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
logs/

# Node/JS artifacts (if any)
node_modules/
dist/
build/

# Coverage reports
lcov.info
coverage/
tarpaulin-report.html
tarpaulin-report.json
*.profraw
*.profdata

# Node / JS tooling
dashboard/node_modules/
server/node_modules/
services/*/node_modules/
tools/*/node_modules/
node_modules/
server/dist/
dashboard/dist/
services/*/dist/
.npm/
.yarn/
.pnp.js
.pnp.cjs

# Mutation testing output (cargo-mutants)
mutants.out/
mutants.out.lock

# Runtime backup archives/snapshots (scripts/backup.sh, scripts/test_backup_restore.sh)
/backups/

# Indexer SQLite database (tools/indexer, services/indexer) — runtime state, not source
indexer.db
indexer.db-*
*.db-journal
*.db-wal
*.db-shm

# Canary deployment decision history (scripts/canary_deploy.sh)
/deploy/

# Node modules / build output for TS services
services/indexer/node_modules/
services/indexer/dist/

# Restore-drill state files (scripts/restore.sh idempotent/resumable scenario 6)
*.state.json

# TypeScript build output
*.tsbuildinfo
*.d.ts.map

# Temporary / OS files
*.tmp
*.temp
.DS_Store
ehthumbs.db

# Kiro agent scratch files
.kiro/agents/

# Benchmark / profiling output
perf.data
perf.data.old
flamegraph.svg
*.perf

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# proptest regression files (auto-generated, committed only when intentional)
# Uncomment the line below to exclude them:
# proptest-regressions/

# Cargo doc output
/doc/

# Generated Soroban bindings / TypeScript SDK artifacts
bindings/
sdk/

# Vitest / Jest coverage reports
coverage/
*.lcov
*.pyo

# Cargo lock (keep for binaries, optional for libs — keeping for reproducibility)
# Cargo.lock  # intentionally tracked
